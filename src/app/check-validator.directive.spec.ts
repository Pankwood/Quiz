import { CheckValidatorDirective } from './check-validator.directive';

describe('CheckValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
